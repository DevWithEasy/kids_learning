import React, { useEffect, useState } from "react";

const Home = () => {
    const [view, setView] = useState(true)
    useEffect(()=>{
        setTimeout(()=>{

        },2500)
    })
    return (
        <div
            className=""
        >
            
        </div>
    );
};

export default Home;
